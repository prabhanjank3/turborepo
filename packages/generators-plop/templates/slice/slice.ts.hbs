import { createSlice, PayloadAction } from '@reduxjs/toolkit';

export interface {{pascalCase name}}State {
  value: number;
}

const initialState: {{pascalCase name}}State = {
  value: 0,
};

export const {{camelCase name}}Slice = createSlice({
  name: '{{camelCase name}}',
  initialState,
  reducers: {
    increment: (state) => {
      state.value += 1;
    },
    decrement: (state) => {
      state.value -= 1;
    },
    incrementByAmount: (state, action: PayloadAction<number>) => {
      state.value += action.payload;
    },
  },
});

export const { increment, decrement, incrementByAmount } = {{camelCase name}}Slice.actions;
export default {{camelCase name}}Slice.reducer;
