module.exports = {
  semi: true, // Keep semicolons
  singleQuote: true, // Use single quotes
  trailingComma: 'all', // Add trailing commas where possible
  arrowParens: 'always', // Always include parens in arrow functions (for consistency)
  printWidth: 80, // Wrap lines that exceed 80 characters
  tabWidth: 2, // Set tab width (optional, but consistent with your ESLint)
  useTabs: false, // Prefer spaces over tabs
  bracketSpacing: true, // Add spaces between brackets in object literals
  jsxBracketSameLine: false, // Put the `>` of a multi-line JSX element at the end of the last line
};
